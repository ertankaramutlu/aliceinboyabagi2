PUBLIC_SANITY_PROJECT_ID=aubx2pik
PUBLIC_SANITY_DATASET=production
# SANITY_API_READ_TOKEN=   # dataset private ise build için; tarayıcıya koyma

# Studio Save → Vercel rebuild:
# 1. Vercel → Project Settings → Git → Deploy Hooks → Create hook (Production, main)
# 2. Sanity → manage → API → Webhooks → that hook URL
#    trigger: Create, Update, Delete · filter: _type == "post"
